(** The lexer.

    [lex s] turns a source string into a list of tokens, always ending in
    [Token.EOF].

    This is the file you implement for Phase 1. The signature is fixed by the
    phase-1 test suite (see [test/test_phase_1_lexer.ml]); the body is yours. *)

let lex (_source : string) : Token.t list =
  failwith "Lexer.lex: not implemented yet — Phase 1 is up to you"
